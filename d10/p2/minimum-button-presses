#!/usr/bin/env ruby

require "pry"
require "pry-byebug"

require_relative "machine_parser"

machines = MachineParser.parse("inputs_test2.txt")

puts machines.map(&:minimum_pushes_required)
puts machines.map(&:minimum_pushes_required).sum
