[didn't work] NOTE!: instead of driving smallest joltage to 0, drive any odd joltage to an even joltage??
[didn't work] maybe always choose path with fewest odd joltages with hopes of a 2x multiplier to cut problem in half?

[didn't work] Pick target joltage index that appears in the most buttons first instead of the
joltage index of the smallest joltage value.

* Get index of smallest joltage for biggest buttons
* write out results and use them as new maxes for the top-level machines if we are too high on pushes!
* put inputs text files in their own directory to reduce top-level clutter?
* pass in cache name to script?
* move caches to tmp/ ?
* if there's non-overlapping groups of buttons, split them into separate machines??
* Choose joltage index from button in most groups??

* bummer that passing a block to a method that doesn't take a block silently ignores
  the block instead of raising


* idea... iterate through buttons and use last joltage index found? Idea is it would be
  furthest from the biggest buttons increasing chances that they can split?
* oh, how about we take the joltage indices not in the biggest button, and use the last one we find of those?


left off debugging this: rm decouple_cap_and_min_bug.cache*; ./minimum-button-presses


compacting cache thoughts?

hmmmm... worth trying?
if a machine is nil at multiple caps... keep the biggest cap, delete the others
if a machine is non-nil at multiple caps, keep the biggest??

maybe not worth trying...
