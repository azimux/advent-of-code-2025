#!/usr/bin/env ruby

# require "pry"
# require "pry-byebug"

require_relative "network"

inputs_file = "inputs_test.txt"
# inputs_file = "inputs.txt"
wait_on_cache = inputs_file != "inputs.txt"

network = Network.new(inputs_file, start_at: :svr, end_at: :out, wait_on_cache:)
puts network.answer

network.join
